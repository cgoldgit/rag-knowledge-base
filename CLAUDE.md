# 项目 CLAUDE.md — 知识库问答系统开发手册

> 本文件是项目的"使用手册 + 开发宪章"。任何开发会话（包括 Claude Code）都应先读本文档，再动手工作。

## 一、项目概述

- **项目类型**：毕业设计
- **项目名称**：基于 LangChain 的 RAG 企业级知识库问答系统
- **应用场景**：电商平台商品问答。知识库存放商品相关信息（商品详情、说明书、售后政策等），系统帮助用户回答关于商品的问题
- **项目所有者**：cgold（**非技术用户**，初次使用 Claude Code）
- **交互方式**：用户在浏览器中登录、管理知识库、与系统问答

## 二、技术架构

### 技术栈清单

| 角色 | 技术 | 职责（通俗解释） |
|------|------|------------------|
| 后端框架 | **FastAPI**（Python 3.12） | 网站的"大脑"，处理所有请求逻辑 |
| AI 框架 | **LangChain** | 组装 RAG 流程的"乐高积木"，毕设必用 |
| 前端 | **Vue 3 + Element Plus** | 用户在浏览器看到的界面 |
| 主数据库 | **MySQL 8.4** | 存用户、会话、知识库信息的"档案柜" |
| 向量数据库 | **Chroma** | 存文档片段"数字指纹"的"索引册" |
| 缓存 | **Redis** | 加速器：登录凭证、热门问答缓存、限流 |
| 对话模型 | **DeepSeek API**（deepseek-v4-pro） | 回答问题的"专家" |
| 向量模型 | **硅基流动 BGE-M3** | 给文字做"数字指纹" |
| 重排序模型 | **硅基流动 BGE-reranker-v2-m3** | 检索结果精排，提高准确率 |

### 系统架构（文字版）

```
浏览器（用户操作界面）
    ↓
Vue 3 前端（登录 / 知识库管理 / 问答界面）
    ↓ HTTP 请求
FastAPI 后端（业务逻辑 + LangChain RAG 流程）
    ├── MySQL —— 用户、会话、文档元数据
    ├── Redis —— 登录凭证、缓存、限流
    ├── Chroma —— 文档片段向量（数字指纹）
    ├── DeepSeek —— 生成回答
    └── 硅基流动 —— 向量化 + 重排序
```

### RAG 核心流程（系统的心脏）

```
文档上传 → 解析文本 → 切成小片段（分块）
→ 每段转成"数字指纹"（向量化）→ 存入 Chroma

用户提问 → 问题也转成"数字指纹"
→ 从 Chroma 找出最相似的片段（检索）
→ 重排序精挑最相关的 → 片段 + 问题交给 DeepSeek
→ 大模型只依据知识库内容回答，并标注引用来源
```

## 三、项目目录结构（规划）

```
langchain-RAG/
├── CLAUDE.md          ← 本文件
├── .env               ← 秘密配置（API 密钥，绝不提交 Git）
├── .gitignore
├── backend/           ← 后端（FastAPI）
│   ├── app/
│   │   ├── main.py            # 后端入口
│   │   ├── config.py          # 配置读取（.env）
│   │   ├── database.py        # MySQL 连接
│   │   ├── models/            # 数据库表模型
│   │   ├── routers/           # 接口路由（auth/知识库/问答/会话）
│   │   ├── services/          # 业务逻辑（RAG、文档处理）
│   │   └── schemas/           # 数据格式定义
│   ├── requirements.txt       # Python 依赖清单
│   └── .env                   # 后端读的密钥（或复用根目录）
└── frontend/          ← 前端（Vue 3）
    ├── src/
    │   ├── views/             # 页面（登录/注册/问答/知识库管理）
    │   ├── components/        # 组件
    │   ├── router/            # 页面路由
    │   ├── api/               # 后端接口调用
    │   └── store/             # 登录状态管理
    └── package.json
```

## 四、环境与账号信息

### 已安装的软件服务

| 软件 | 位置 | 服务名 | 说明 |
|------|------|--------|------|
| MySQL 8.4.5 | C:\tools\mysql-8.4.5-winx64 | MySQL84 | 已注册开机自启 |
| Redis | C:\tools\redis | Redis | 已注册开机自启 |

### 数据库账号（本地开发）

- **root 管理员**：root / root123456
- **项目专用账号**：rag_user / rag123456（应用只用它，权限仅限 rag_kb 库）
- **项目数据库**：rag_kb（utf8mb4 中文支持）

### API 密钥（在项目根目录 .env 中，严禁外泄）

- DeepSeek：对话模型（deepseek-v4-pro / deepseek-v4-flash）
- 硅基流动：向量模型（BAAI/bge-m3）+ 重排序模型（BAAI/bge-reranker-v2-m3）

### 端口约定

| 服务 | 端口 |
|------|------|
| MySQL | 3306 |
| Redis | 6379 |
| 后端 FastAPI | 8000 |
| 前端 Vue | 5173 |

### 管理员账号

- **admin / 123456**（预置，只有管理员能管理知识库）

## 五、开发规范与注意事项

### 安全红线（最重要）

1. **.env 含 API 密钥，绝不提交 Git、绝不上传网络**（.gitignore 已排除）
2. 密钥相当于"银行卡密码"，任何人索要都不可提供
3. 数据库密码、管理员密码不写入普通代码文件

### 沟通与协作规范

1. **始终使用中文交流**
2. 用户 cgold 不懂编程，任何技术概念必须用生活化比喻通俗解释
3. **技术决策必须列 2~4 个方案**，说明优缺点，给出推荐，等用户选择后再动手
4. **Git 操作（提交、推送、分支）必须事先解释并征得用户同意**
5. 如实报告进度，不夸大不隐瞒

### 开发顺序（启动系统的步骤）

1. 确认 MySQL、Redis 服务在运行（Windows 服务，开机自启）
2. 启动后端：`cd backend && conda activate rag-kb && python -m uvicorn app.main:app --reload`（端口 8000）
   - 或直接双击运行 `backend/run.bat`（已配置好 conda 环境）
3. 启动前端：`cd frontend && npm run dev`（端口 5173）
4. 浏览器访问 http://localhost:5173

### Python 环境说明（已迁移到 conda）

- **conda 环境名**：`rag-kb`（Python 3.12，位于 `C:\Users\cgold\miniforge3\envs\rag-kb`）
- 旧的 `backend/.venv` 已删除，**一律使用 conda 环境**
- 迁移后依赖版本注意：**bcrypt 已固定为 4.2.1**（5.0 与 passlib 不兼容，升级需谨慎）

### 功能权限（重要需求）

- **管理员（admin）**：可进入知识库管理页面（上传/删除文档）
- **普通用户**：只能进行知识库问答，看不到管理入口
- 每个用户有独立会话，历史记录持久保存，随时找回

## 六、性能优化与答辩亮点（已规划）

1. **混合检索**：向量检索 + 关键词检索合并，双保险不漏
2. **重排序（Rerank）**：检索结果先粗筛再精排
3. **流式输出**：回答打字机效果，降低等待感
4. **缓存策略**：热门问题秒回，重复提问不重复花钱
5. **异步文档处理**：上传大文件不卡界面
6. **数据库优化**：连接池、索引、分页
7. **接口限流**：防止恶意刷接口
8. **多轮对话优化**：结合上下文理解追问
9. **检索兜底**：知识库没有相关内容时明确说明，不硬编答案

## 七、当前进度

- ✅ 环境安装：MySQL、Redis
- ✅ API 密钥：DeepSeek + 硅基流动（已验证）
- ⏳ 待开始：项目骨架（后端 + 前端）、注册登录、知识库管理、RAG 问答
